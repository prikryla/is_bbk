# Enable stateless CSRF protection for forms and logins/logouts
framework:
    form:
        csrf_protection:
            enabled: true
            field_name: _token

    csrf_protection:
        enabled: true
